import * as React from 'react';
export interface DialogFooterProps<T> extends React.HTMLProps<T> {
    className?: string;
    tag?: string;
}
declare const DialogFooter: <T extends {} = HTMLElement>(props: DialogFooterProps<T>) => React.ReactElement<T>;
declare type DialogFooter<T> = React.ReactElement<T>;
export default DialogFooter;
