import * as React from 'react';
export interface DialogTitleProps<T> extends React.HTMLProps<T> {
    className?: string;
    tag?: string;
    id?: string;
}
declare const DialogTitle: <T extends {} = HTMLHeadingElement>(props: DialogTitleProps<T>) => React.ReactElement<T>;
declare type DialogTitle<T> = React.ReactElement<T>;
export default DialogTitle;
